import { AbbriviatePipe } from './abbriviate.pipe';

describe('AbbriviatePipe', () => {
  it('create an instance', () => {
    const pipe = new AbbriviatePipe();
    expect(pipe).toBeTruthy();
  });
});
